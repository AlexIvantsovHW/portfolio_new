import { HeadMeta } from "../widgets/HeadMeta/HeadMeta";

export default function Home() {
  return (
    <>
      {/*      <HeadMeta
        title={metaData.mainPage.title}
        description={metaData.mainPage.description}
        keywords={metaData.mainPage.keywords}
      /> */}
      <main
        className={
          "flex flex-col gap-[40px] px-[16px] md:px-[32px] xl:px-[64px] xxl:px-[272px] py-[15px]  md:w-full "
        }
      >
        Test
      </main>
    </>
  );
}
